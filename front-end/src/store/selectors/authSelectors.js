export const getUsername = (state) => state.authSlice.username;
export const getToken = (state) => state.authSlice.token;
export const getRole = (state) => state.authSlice.role;
export const getUserData = (state) => state.authSlice;
